module.exports = {
    events: require('./order-events'),
    service: require('./order-service'),
    schemas: require('./schemas'),
    Error: require('./order-error'),
    ENUM: require('./order-enum')
}